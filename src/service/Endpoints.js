const HOST = "http://localhost:8080";

export const Endpoints = {
  REGISTRATION: HOST + "/Registrazione",
  LOGIN: HOST + "/Login",
  UPDATE_EMAIL: HOST + "/ChangeEmail",
  UPDATE_PASSWORD: HOST + "/ChangePassword",
  GET_DISHES: HOST + "/Dishes",
  GET_USER: HOST + "/User",
  LIST_DISHES: HOST + "/ListDishes",
};
